// Compose stability contract for the shared sense card. Sense is an effectively-immutable
// domain data class living in a non-Compose module, so the compiler cannot infer it.
app.sensee.lexicon.domain.Sense
